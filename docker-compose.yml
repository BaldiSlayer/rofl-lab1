version: '3'
services:
  llm:
    build:
      dockerfile: dockerfiles/llm.dockerfile
    container_name: llm
  kbase:
    build:
      dockerfile: ./dockerfiles/kbase.dockerfile
    container_name: kbase
  interpret:
    build:
      dockerfile: ./dockerfiles/interpret.dockerfile
    container_name: interpret
